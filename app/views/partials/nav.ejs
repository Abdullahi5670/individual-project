<nav class="navbar navbar-expand-lg bg-white border-bottom sticky-top">
  <div class="container">
    <a class="navbar-brand fw-bold" href="/sessions">StudyGroup Finder</a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#nav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="nav">
      <ul class="navbar-nav me-auto">
        <li class="nav-item"><a class="nav-link" href="/sessions">Sessions</a></li>
        <% if (user) { %>
          <li class="nav-item"><a class="nav-link" href="/sessions/new">New Session</a></li>
        <% } %>
      </ul>

      <div class="d-flex gap-2 align-items-center">
        <% if (user) { %>
          <span class="badge text-bg-light border">Hi, <%= user.name %></span>
          <form method="POST" action="/logout" class="m-0">
            <button class="btn btn-outline-secondary btn-sm" type="submit">Logout</button>
          </form>
        <% } else { %>
          <a class="btn btn-outline-primary btn-sm" href="/login">Login</a>
          <a class="btn btn-primary btn-sm" href="/signup">Sign Up</a>
        <% } %>
      </div>
    </div>
  </div>
</nav>
